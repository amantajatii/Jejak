# @jejak/domain

Canonical Jejak JSON Schema, generated TypeScript types, shared sandbox fixtures,
and cross-language byte vectors.

Source files live under `schemas/`. Files under `src/generated/` are generated and
must never be edited manually.

```bash
pnpm --dir packages/domain validate
pnpm --dir packages/domain generate
pnpm --dir packages/domain test
```
