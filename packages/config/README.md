# @jejak/config

Shared TypeScript configuration for Jejak Node.js packages.

Backend packages extend `@jejak/config/tsconfig/node.json`. Frontend adoption is
owned by FE and is intentionally not forced by the backend workspace scaffold.
